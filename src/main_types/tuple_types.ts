{
  const skills: [number, string] = [1, "Dev"];
  const id = skills[0];
  const name = skills[1];

  const [skillId, skillName] = skills;

  const arr: [number, string, ...boolean[]] = [1, "Dev", true, true];
}
